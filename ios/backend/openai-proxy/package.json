{
    "name": "readymcat-openai-proxy",
    "version": "0.1.0",
    "private": true,
    "description": "ReadyMCAT serverless proxy: keeps the OpenAI key server-side and generates teach-on-miss ladders for the iOS app.",
    "license": "AGPL-3.0-or-later",
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "deploy": "wrangler deploy",
        "cf-typegen": "wrangler types",
        "typecheck": "tsc --noEmit",
        "smoke": "bash scripts/smoke.sh"
    },
    "devDependencies": {
        "typescript": "^5.6.0",
        "wrangler": "^4.0.0"
    }
}
